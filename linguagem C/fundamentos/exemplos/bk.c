#include <stdio.h>

int main()
{
    // entrada de dados
    float rodeio = 24.99;
    float refri = 7.99;
    float batataGrande = 14.99;
    float casquinha = 4.99;
    float cupom = (7/100),
    
    float ValorInicial = rodeio + refri + batataGrande + casquinha + cupom 
    float desconto = (7/100* );
    float valorfinaltotal = (CupomSec - valor);
    // saida

    printf("o total é R$ %.2f", valorfinaltotal);
    return 0;
}