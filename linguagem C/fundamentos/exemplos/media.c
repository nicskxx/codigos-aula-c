#include <stdio.h>
int main (){
    //variaveis delcaradas
    // entradas
    int nota1= 8;
    int nota2 = 7;

    //processamento
    int media = (nota1 + nota2) / 2;

    //saida
printf( "A media e%d", media);

    return 0;
} 